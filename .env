# API Configuration
# The frontend uses Vite's proxy to forward /api requests to the backend
# For development, the proxy in vite.config.ts handles this
# For production, set the full API URL here (e.g., https://your-backend.onrender.com)

VITE_API_BASE_URL=
