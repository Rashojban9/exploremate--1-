version: '3.8'

services:
  # Frontend only (requires backend services to be running separately)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exploremate-frontend
    ports:
      - "80:8080"
    environment:
      - VITE_API_BASE_URL=http://localhost:9080
    restart: unless-stopped
